# IMPLEMENTATION_PLAN.md

## Текущий статус

| Этап | Статус |
|---|---|
| Stage 1. Анализ и архитектура | ✅ завершён |
| Stage 2. MVP | ✅ завершён |
| Stage 3. Усиление проверок | ✅ завершён |
| Stage 4. UX и поддерживаемость | 🔄 частично |
| Stage 5. Тестирование и выпуск | ⬜ не начат |

---

## Stage 1. Анализ и архитектура ✅

Спроектировано:
- структура Excel-книги (14 листов),
- состав карточки документа,
- справочники и матрицы проверки,
- стратегия Word-шаблонов,
- состав VBA-модулей, классов и форм,
- сценарии работы пользователя.

**Результат:** `architecture.md`, `AGENTS.md`, `IMPLEMENTATION_PLAN.md`, `DOCUMENT_TYPES.md`, `VALIDATION_RULES.md`.

---

## Stage 2. MVP ✅

Реализовано:
- Excel-книга со структурой проекта (`modWorkbookSetup`);
- карточка документа (`clsDocumentCard`, 31 поле);
- 2 типа документов (RI и EA);
- `RepairInstruction.dotx` — полноценный OOXML-шаблон на базе реального документа `A.095.04.00000.078.0699_example.docx`; 20 маркеров;
- `EngineeringAnalysis.dotx` — текстовая заглушка (требует замены);
- создание документа из шаблона (`modWordAutomation`);
- подстановка 20+ маркеров реквизитов;
- базовая проверка полей;
- журналирование (`modLogging`);
- экспорт DOCX/PDF.

---

## Stage 3. Усиление проверок ✅

Добавлено:
- проверка незаменённых маркеров `{{...}}`;
- проверка незаполненных инженерных секций `<<...>>`;
- проверка мусорных заглушек (TBD/XXX/??? + `MatchWholeWord` для "sample"/"draft");
- проверка обязательных разделов из `rules_required_sections`;
- матрица clauses для Engineering Analysis;
- матрица разделов для Repair Instruction;
- расширенная `ValidateRequiredFields` (14 правил);
- форма отчёта `frmValidationReport` с кнопкой Close и счётчиками.

---

## Stage 4. UX и поддерживаемость 🔄

Сделано:
- форма `frmDocumentCard` расширена до 31 поля;
- `LoadActiveRowIntoControls` загружает по имени колонки (не по позиции);
- понятные сообщения об ошибках с указанием первопричины;
- tooltips для каждого поля (`GetFieldHint`);
- кнопка **Field Help** с описанием всех полей.

Остаётся:
- полноценный шаблон `EngineeringAnalysis.dotx`;
- кнопки на листе `ui_dashboard` для основных сценариев;
- визуальная группировка полей формы (Aircraft / Component / Document / Persons).

---

## Stage 5. Тестирование и выпуск ⬜

Нужно:
- пройти чек-лист приёмки из `docs/test_plan.md`;
- создать тестовые данные для EA (заполнить `ea_clause_matrix_sample.csv`);
- задокументировать known limitations;
- зафиксировать версию как `v1.0`.
